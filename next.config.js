/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
/*   swcMinify: true, isso nao é mais usado no nextjs, a partir da versao 14 acredito eu... Não tem mais necessidade */
}

module.exports = nextConfig
